package com.resolum.intiva.platform.iam.domain.model.valueobjects;

/**
 * The RoleTypes enum defines the different types of roles that can be assigned to users in the system.
 * Each role type represents a specific set of permissions and access levels.
 */
public enum RoleTypes {
    ADMIN,
    USER,
    GUEST
}
