package com.resolum.intiva.platform.iam.domain.model.commands;

/**
 * Command to seed roles in the system. This command can be used to initialize the system with default roles or to reset the roles to a known state.
 */
public record SeedRolesCommand() {
}
